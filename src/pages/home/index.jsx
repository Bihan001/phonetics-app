import { Box } from '@mui/material';
import Translator from 'layouts/translator-main';

const Home = () => {
  return (
    <Box sx={{ mt: 5 }}>
      <Translator />
    </Box>
  );
};

export default Home;
